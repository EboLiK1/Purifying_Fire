using UnityEngine;

public class GameEntryPoint : MonoBehaviour
{
    //public static GameEntryPoint Instance { get; private set; }

    //public GameInput GameInput { get; private set; }
    //public Game Game { get; private set; }
    //public PlayerBindings PlayerBindings { get; private set; }
    //public AudioManager AudioManager { get; private set; }
    //public UIManager UIManager { get; private set; }

    //private void Awake()
    //{
    //    if (Instance == null)
    //    {
    //        Instance = this;
    //        DontDestroyOnLoad(gameObject);
    //    }
    //    else
    //        Destroy(gameObject);

    //    GameInput = new GameInput();

    //    PlayerBindings = GetComponentInChildren<PlayerBindings>();
    //    AudioManager = GetComponentInChildren<AudioManager>();
    //    UIManager = FindObjectOfType<UIManager>();

    //    if (Game != null)
    //        Game.Initialize(GameInput);

    //    if (PlayerBindings != null)
    //        PlayerBindings.Initialize(GameInput);

    //    if (UIManager != null)
    //        UIManager.Initialize(GameInput);

    //    if (AudioManager != null)
    //        AudioManager.Initialize();
    //}
}