using UnityEngine;

public class PlayerCombat : MonoBehaviour
{
    //[SerializeField] private Transform _attackZone;
    //[SerializeField] private float _attackRadius;
    //[SerializeField] private LayerMask _enemy;

    //public void Damage()
    //{
    //    Collider2D[] detectedObject = Physics2D.OverlapCircleAll(_attackZone.position, _attackRadius, _enemy);

    //    foreach (Collider2D collider in detectedObject)
    //    {
    //        Enemy enemyHealth = collider.GetComponent<Enemy>();
    //        enemyHealth.Damage();
    //    }
    //}

    //private void OnDrawGizmos()
    //{
    //    Gizmos.color = Color.red;
    //    Gizmos.DrawWireSphere(_attackZone.position, _attackRadius);
    //}
}
